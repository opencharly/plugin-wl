# 2026.253.1019 — feat(wl): the ocr method — the screen text assertion (grim at 2x + tesseract --psm 11)

## Summary

The `ocr` method on the `wl:` verb — the screen text assertion, the charly-native equivalent of upstream omarchy's `screen_contains` (the acceptance-test helper): the capture at 2x scale + tesseract `--psm 11`, the small-caption fix. The optional artifact path saves the capture for the media lane. A missing tesseract in the venue is a clear error, never a faked pass. The capture switch is the pure `ocrCaptureCmd` helper (the same seam wlScreenshot uses).

The method completes the charly-native translation of the omarchy acceptance suite's visual assertions (the Wave 1 plugin delta of the omarchy-test-corpus equivalence plan).

## How tested (pasted)

**The live proof — the ocr method on the eval golden (the real shell surface):**

```
$ charly update check-omarchy-eval-base-inst && charly check live check-omarchy-eval-base-inst
  PASS  check the ocr reads the shell's update prompt off the screen (grim at 2x + tesseract; the always-on shell unit renders it)  [ocr-screen-text]  The screen text contains "Update System"
40 steps: 28 passed, 0 failed, 12 skipped
```

The changed path executed live on the DISPOSABLE target check-omarchy-eval-base-inst (disposable: true — the rebuild is the authorized `charly update`): the ocr captured the golden's screen (grim at 2x), ran tesseract --psm 11, and asserted the real shell surface's text ("Update System") — the corpus candy's ocr step (eval-omarchy#60). An earlier probe attempt (the hyprctl-notify banner) surfaced the font/arg-order issues honestly as FAILs before the fix.

**The unit suite:**

```
$ go test -count=1 -run "TestOcrContains|TestWlOcrRequiredModifiers|TestOcrCaptureCmd" -v .
--- PASS: TestOcrContains (0.00s)
--- PASS: TestWlOcrRequiredModifiers (0.00s)
--- PASS: TestOcrCaptureCmd (0.00s)

$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-wl/candy/plugin-wl	0.004s
$ go vet ./... && gofmt -l .
VET-OK
FMT-CLEAN
```

TestOcrCaptureCmd exercises the ACTUAL capture-switch helper (ocrCaptureCmd, now PURE over its three inputs — the tool probes + the discovered output name): pixelflux wins the priority, grim carries the 2x scale + the quoted discovered output, and a venue with neither is a clear error — the test fails on the pre-helper code. TestOcrContains locks the text assertion (grep -F -i semantics); the requiredModifiers lock the text requirement.

## Rulebook compliance

- R0: the wl skill loaded; the method contract (requiredModifiers + the schema enum) followed.
- R1: the acceptance suite's OCR assertions traced to the missing verb surface; the hyprctl-notify probe failures RCA'd to the arg order + the expiry before the ocr ran.
- R3: the ocr method reuses the existing wlScreenshot capture seam (the same grim/pixelflux switch, now the pure helper).
- R4: no workarounds — a missing tesseract is a clear error, never a faked pass.
- R6: no force-push, no hook bypass, PR-only landing.
- R10: the live proof + the unit tests pasted.

## Change classification

- Change class: feat (the ocr method)
- Verification gate: the live proof + the unit tests (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*