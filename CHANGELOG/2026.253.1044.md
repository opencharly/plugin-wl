# 2026.253.1044 — feat(wl): the ocr scale is the authored choice (0 = the native resolution)

## Summary

The ocr method's scale becomes the authored choice: 0/absent captures at the NATIVE resolution (the default — the large-text venues like the omarchy shell surfaces read best there), and `scale: 2` preserves the upstream screen_contains small-caption fix. ocrCaptureCmd is pure over the four inputs; the test locks the switch including the scale.

## How tested (pasted)

**The live proof (the native-res ocr on the DISPOSABLE target check-omarchy-eval-base-inst, disposable: true):**

```
$ charly check live check-omarchy-eval-base-inst
  PASS  check the ocr reads the shell's update prompt off the screen (the native-res capture + tesseract ...)  [ocr-screen-text]  The screen text contains "Update System"
44 steps: 32 passed, 0 failed, 12 skipped
```

(The 2x-scale capture made tesseract fail on this venue — RCA'd live; the native-res capture reads the surfaces.)

**The unit suite:**

```
$ go test -count=1 -run TestOcrCaptureCmd -v .
--- PASS: TestOcrCaptureCmd (0.00s)
$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-wl/candy/plugin-wl	0.004s
$ go vet ./... && gofmt -l .
VET-OK
FMT-CLEAN
```

TestOcrCaptureCmd locks the scale: the native (0) capture carries NO -s, the scaled (2) carries -s 2, pixelflux wins the priority, and a venue with neither is a clear error.

## Rulebook compliance

- R0: the wl skill loaded.
- R1: the 2x-scale tesseract failure RCA'd live on the golden (the scaled capture read garbage; the native capture reads the surfaces).
- R3: the scale is one input on the existing ocr seam.
- R4: no workarounds.
- R6: no force-push, no hook bypass, PR-only landing.
- R10: the live proof + the unit tests pasted.

## Change classification

- Change class: feat (the authored ocr scale)
- Verification gate: the live proof + the unit tests (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*