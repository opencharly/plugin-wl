# 2026.242.0533 — fix: stamp the root manifest at the schema this repo's CI charly supports

## Summary

Repairs a regression I introduced in the previous PR.

The root `charly.yml` I added stamped schema **`2026.240.1943`** (the current HEAD), but
this repo's `deploy.yml` pins charly **`v2026.238.1242`**, whose maximum supported schema is
**`2026.232.0520`**. So the `candy` workflow started failing on `main`:

```
config schema 2026.240.1943 is newer than this charly supports (max 2026.232.0520)
```

This stamps `2026.232.0520` — the value the 226 peer candy repos already carry, and the one
this repo's pinned charly accepts.

Nothing else changes: the candy files under `candy/` are untouched, because the schema
requirement applies to the project **root** manifest only.

Refs opencharly/charly#459.

## How tested

Against a binary built from **this repo's exact pinned charly tag** (`v2026.238.1242`), not
a newer local build — that mismatch is what caused the regression:

```
$ charly box validate     # root stamped 2026.232.0520
exit=0                    # accepted

$ charly box validate     # root stamped 2026.232.0521
config schema 2026.232.0521 is newer than this charly supports (max 2026.232.0520)

$ charly box validate     # root stamped 2026.240.1943  (what the previous PR shipped)
config schema 2026.240.1943 is newer than this charly supports (max 2026.232.0520)
```

**The gate is still real at the corrected stamp** — quiet acceptance would be worthless.
With `- verb:dbus` changed to `- notaclass:dbus` in a plugin candy:

```
charly: error: command "validate": 2 validation errors:
  candy "plugin-dbus": CUE schema: .../candy/plugin-dbus/charly.yml:
  candy "plugin-dbus": 2 errors in empty disjunction:
```

So the repo still gets the gate the previous PR was for, and its `candy` workflow goes green
again.

## Rulebook compliance

- **R0 (skills)** — `/charly-internals:plugin`, `/charly-internals:git-workflow`.
- **R1 (RCA)** — root-caused before fixing: the stamp must match the schema HEAD of the
  charly each repo's CI pins, and I used the newest HEAD without checking the pin. Confirmed
  by building that exact tag and testing three stamps against it.
- **R2 (no split)** — the regression is fixed everywhere it landed, in the same sweep, not
  deferred.
- **R3 (no duplication)** — one stamp value, matching the peer corpus, rather than a
  per-repo special case.
- **R4 (no workarounds)** — the stamp is corrected; the gate is not disabled, skipped or
  made non-blocking to hide the failure.
- **R4a (fix the product first)** — N/A — my own regression, not a product defect.
- **R5 (hard cutover)** — the wrong value is replaced outright; nothing is left behind.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass.
- **R7 (prove the gate)** — the three-stamp matrix and the malformed-manifest check above
  were run against the pinned tag's binary.
- **R9 (deployed binary)** — N/A — no binary or runtime path.
- **R10 (fresh disposable proof)** — N/A — cannot reach a running deployment; the validate
  matrix fully observes the change.
- **RDD** — the assumption that broke this ("HEAD stamp is fine everywhere") is exactly what
  is now measured rather than assumed.
- **ADE / SDD** — N/A — no install content, no schema or generated code.
- **Concurrency mandate** — N/A — a one-line change.
- **Architecture gate / placement** — correct: the repo declares its own shape at a version
  its own CI can read.

## Change classification

- **Change class:** regression fix — one line in the root manifest.
- **Verification gate:** `charly box validate` under the repo's pinned charly, across three
  stamps plus a deliberately malformed manifest.
- **Attribution tier:** `fully tested and validated` — exercised against the exact toolchain
  this repo's CI uses, with output retained above.

*Assisted-by: Claude Code claude-opus-5[1m] (fully tested and validated)*