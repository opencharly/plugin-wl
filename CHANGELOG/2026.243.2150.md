# 2026.243.2150 — docs(skills): tighten the 2 wl skill descriptions to trigger-style

## Summary

Tighten the `description:` frontmatter of the wl and wl-overlay skill entities to trigger-style index lines ("Use when X"). The descriptions are the skill index injected into every harness system prompt (progressive disclosure: only name + description + location are always in context). Shorter, trigger-style descriptions shrink the index without touching skill names, bodies, or discoverability — **zero capability reduction**: both skills remain discoverable and loadable, and every trigger signal is preserved.

Description bytes: ~760 → 587.

## How tested

- `python3` YAML parse of the edited `charly.yml`: OK — both skill entities parse, descriptions verified.
- `git diff` review: only `description:` blocks changed (4 insertions / 4 deletions); no structural key touched; no body content changed.
- The change is metadata-only prose; no behavior, schema, or build surface affected.

## Rulebook compliance

- **R3** — no duplication: each description is a single canonical index line.
- **R5** — no stale references: every new description preserves the original trigger signal.
- **A3 (attribution):** the change is prose-only metadata (skill `description:` fields) with ZERO behavior change and no pins/scripts changed. The umbrella attribution table (AGENTS.md) defines the docs-only class as "forbidden if pins/scripts changed" — no pins or scripts changed here, so `documentation reviewed` is the honest tier for this metadata-prose change.
- No pins, no scripts, no schema changes, no direct push to main.

## Change classification

- **Change class:** docs-only (skill-description metadata prose; zero behavior change)
- **Verification gate:** YAML parse + diff review
- **Attribution tier:** `documentation reviewed`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*